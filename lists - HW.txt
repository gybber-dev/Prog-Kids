домашнее задание.

1. В переменную "blocksList" поместить пустой массив
2. Создать переменные x, y, z и положить в них рандомные значения в пределах
нашего участка. Используй для этого функцию random.randint. Например,
x = random.randint(-10, 10)
...
3. На этом пункте у тебя уже есть переменные x,y,z, в которые ты уже поместил
некоторые координаты из нашей карты. Тебе нужно узнать id блока, который
находится по этим координатам, и сохранить этот id в переменную "id".
Используй команду mc.world.getBlock.
4. С помощью метода append, нужно будет добавить получившийся id блока в наш 
массив "blocksList", вот так:
blocksList.append(id).
5. Повтори пункты 2 - 4, так, чтобы в списке "blocksList" лежало 7 значений,
используя циклы.
6. Выведи на экран длину нашего списка "blocksList", используя функцию len:
    len(blocksList)